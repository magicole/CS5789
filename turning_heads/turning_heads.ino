void setup() {
  // put your setup code here, to run once:
  
  // set up random number generators
  // pin set up for servo board
  // pin set up for ping sensor
  // initialize connection to servo board
  // put servos in their starting positions
}

void loop() {
  // put your main code here, to run repeatedly:

  // make sensor reading
  // see if it is within the limit
  // if it is then turn the heads
  // else increment head position by one
  // if heads are looking at eachother
  // if they are then turn them away
}
